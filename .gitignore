# Secrets — never commit (use env or Secret Manager / Cloud Run secrets)
.env
.env.*
!.env.example

# Credentials
*.pem
*-credentials.json
**/sqlAccountCredentials.json
secrets/

# Dependencies
node_modules/

# OS / IDE
.DS_Store
Thumbs.db
